__pycache__/
*.pyc
.venv/
venv/
.env

# Gmail/Calendar OAuth secrets and tokens — never commit these
backend/credentials/*.json
!backend/credentials/.gitkeep

.DS_Store

# Internal design/decision notes — visible locally (no leading dot so it's
# easy to browse), never pushed to GitHub.
claude/

# Real extracted email/deadline data — never commit
backend/db_dumps/

# Lambda deployment zip output (backend/scripts/build_lambda.sh)
backend/build/
